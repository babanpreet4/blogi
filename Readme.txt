-------------------------------------------------------------------------------------------------------------------------

Title:"Blogi"

-------------------------------------------------------------------------------------------------------------------------
Requirements: Nodejs(latest Version),Postgresql, Anaconda Navigator, pgadmin4
packages installed: Fastapi, uvicorn,psycopg2, passlib



Execution Procedure:

1. Open the Sourcecode Folder.

2. In the folder You have two folders (Frontend, Backend)

Backend:  

3. Open the Command Prompt.

4. Change the path which has the folder of backend(example cd Z:\Babanpreet\2025\Product\Product1\CMP_PROJ24017\Sourcecode\Backend)

5. Type the command (python -m uvicorn main:app --reload) and click enter

Frontend:(separate command prompt for both admin and frontend)

8. Repeat the steps from 3 to 4 (example Z:\Babanpreet\2025\Product\Product1\CMP_PROJ24017\Sourcecode\Frontend)

9. Type the command npm i to install the packages

9. After successful installation of packages type the command ( npm start) and click enter

10. Now you have the two terminals for each folders.

-------------------------------------------------